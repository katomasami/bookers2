<div class="col-md-4 col-sm-8 mx-2 mx-sm-auto rounded px-0 my-3">
<h1>Editing Book</h1>

<% if @book.errors.any? %>
  <%=  @book.errors.count %> errors prohibited this obj from being saved:
  <ul>
    <%  @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @book do |f| %>
  <label for="inputName">Title</label>
  <%= f.text_field :title, class: "form-control", autofocus: true, id:"inputName" %>
  <label for="inputName">Opinion</label>
  <%= f.text_area :body, class: "form-control", autofocus: true, id:"inputintroduction" %>
  <p><%= f.submit "Update Book", class: "btn btn-md btn-success mt-3" %></p>
<% end %>

  <%= link_to "Show", book_path(@book) %>
  |
  <%= link_to "Back", books_path %>
</div>